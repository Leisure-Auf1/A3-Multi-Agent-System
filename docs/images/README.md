# Screenshots

Place screenshots here for the A3 product showcase:

```
docs/images/
├── dashboard.png       — Main dashboard with learning progress
├── learning_flow.png   — Student → Profile → Plan → Resources flow
├── multimodal.png      — Gateway generating 7 resource types
├── evaluation.png      — Quiz results with weakness detection
└── tutor_chat.png      — ChatGPT-style streaming tutor interface
```

> Screenshots are not yet generated. Run the application to capture them.

