# A3 Multi-Agent System — Application Dependencies
# Depends on Veritas-Core for runtime infrastructure
#
# Veritas-Core is NOT published on PyPI — install directly from GitHub.
# (Per docs/phase10.1-deployment-design.md §Dependency Strategy)
# Pinned to an exact commit for deterministic, reproducible Docker builds
# (also busts Docker layer cache whenever the pin is bumped).
# Pin = squash-merge of PR #1 (fix: veritas.sdk.adapters wheel packaging).
# Local dev alternative: pip install -e /path/to/Veritas-Core
veritas-core @ git+https://github.com/Leisure-Auf1/Veritas-Core.git@7107dd22b27d2c2d5a28d2e2be9cf3877d459e6c
streamlit>=1.28.0
fastapi>=0.115.0
uvicorn>=0.30.0
